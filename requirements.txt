# Requirements for LeRobot Inference Server
# Install with: pip install -r requirements.txt

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# HTTP Client
requests>=2.31.0

# Image Processing
Pillow>=10.0.0

# Configuration
pyyaml>=6.0.0

# ML/DL (if not already installed with lerobot)
torch>=2.0.0
numpy>=1.24.0

# Optional: For enhanced logging
python-json-logger>=2.0.0

# Note: Make sure lerobot is installed separately
# pip install -e /workspace/lerobot[smolvla]
# or
# pip install lerobot[smolvla]
